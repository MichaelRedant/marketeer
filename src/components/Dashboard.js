import React from "react";
import { Card, CardContent, CardHeader } from "@mui/material";

const Dashboard = () => (
  <Card>
    <CardHeader title="Welkom in het Admin Dashboard" />
    <CardContent>
      Hier kun je berichten, projecten en diensten beheren.
    </CardContent>
  </Card>
);

export default Dashboard;
